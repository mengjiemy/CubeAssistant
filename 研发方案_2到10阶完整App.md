# 魔方学院 · 2-10 阶完整 App 研发方案（上架前全功能）

> 版本：v1 ｜ 日期：2026-09-08 ｜ 定位：一次看清「从当前 v14 3 阶 + 2 阶引擎逻辑，到 2-10 阶完整 App + 上架」的**全量分批路线**，每批可独立交付、可侧载验证、不返工。

---

## 0. 必须摆正的三个现实（杰哥先看这个）

1. **¥688 = 苹果开发者账号年费**，不是 App 内售价。它是上架门票。功能研发全在交钱前做，交钱后主要是接签名/配内购/过审等手续。
2. **一个晚上做不完 2-10 阶**。3 阶降阶法引擎 ≈ 现 App 体量 2-3 倍，是数周级算法工程。硬塞会毁掉已跑通的 v14。→ 所以用**分批**，每批可验、不返工。
3. **4-10 阶"下一步引导至还原"能做到，且体验跟 3 阶无差异**，但解法结构不同（不是一次算全程，而是分阶段 + 实时算下一步）。靠"三道闸"杜绝"走到最后还原不了"。

---

# 一、最终产品形态（一句话）

> **一个 App，支持 2 阶（免费）与 3-10 阶（内购解锁）。任何阶数：扫码/录入乱态 → 统一引导界面一步步转到还原，体验与 3 阶完全一致。合法状态顺畅到还原；非法状态开解前就被拦下重拍，绝不中途死路。**

---

# 二、全量功能地图（分 6 组）

| 组 | 内容 | 侧载可测 | 挡路 |
|---|---|---|---|
| A 引擎 | 2 阶 UI、3 阶优化、4-10 阶降阶引擎 | ✅ | 工作量巨大(最难) |
| B 引导壳 | 跨阶统一引导(轨道/实时两模式)+三道闸 | ✅ | 需结合 A 引擎 |
| C 输入 | 多阶扫码放大识别+多阶合法性校验 | ⚠️ 部分 | 真机摄像头 |
| D 体验 | 手势、新手图卡、公式库接入 | ✅ | — |
| E 数据 | iCloud 云同步 | ⚠️ 需真账号 | 免费号不通 |
| F 商业化+上架 | 真实内购、隐私/元数据、压测 | ❌ | 需 688 后 |

---

# 三、分批研发路线（每批独立交付 & 验证，不返工）

## ▶ 第 1 批（今晚·目标 v15）——多阶"地基 + 统一引导壳 + 2 阶入口"

**目标**：让用户看到"App 已开始多阶"，且引导界面已为将来 7/10 阶定型。

1. **2 阶接入界面**：`CubeModel.order` 接"阶数选择器"，点 2 阶 → 3D 渲染 8 块、转动走 `Cube2x2`、计时/历史/趋势带 `order`。
2. **跨阶统一引导壳**：把 3 阶现 `SolveSession` 的"还原指引 UI"抽成通用组件，定义 `SolveGuidance` 协议——3 阶走轨道模式，高阶走实时模式。**壳定型，高阶只塞实现**。
3. **多阶数据模型**：`SolveRecord` 加 `order`；历史/趋势/备份跨阶分离。
4. **方案文档落档**（本文件即产物之一）。

**交付**：`魔方学院_demo_v15_unsigned.ipa`，侧载可测 2 阶可玩 + 引导壳跑在 3 阶上。

---

## ▶ 第 2 批（短期·多批）——核心攻坚：4 阶降阶引擎 + 三道闸

**目标**：把最难的 4 阶啃下来，验证"降阶 + 每步引导 + 不中途死路"整条链路成立。5/6/7 阶是同一框架延伸。

1. **多阶合法性校验器**（闸门1）：4 阶起校验角/棱/中心可解性与奇偶性。非法→拦下重拍。
2. **4 阶降阶求解器**：中心合并 + 棱合并 + 归并 3 阶坐标 → 复用 `KociembaSolver` 收尾。
3. **阶段化引导 + 自检回溯**（闸门2/3）：拆"拼中心→拼棱→3阶收尾"三阶段，每阶段完成自检、失败回上一存档点；每步基于真实最新状态实时算下一步。
4. **4 阶引导 UI 跑通**：接入统一引导壳，验证"跟 3 阶零差异"。

**交付**：v16 起，侧载可测 4 阶"拍照→下一步引导至还原"。

---

## ▶ 第 3 批 —— 5/6/7 阶 + 8/9/10 阶延伸

**目标**：把 4 阶框架延伸，覆盖 5-10 阶。
- 5 阶：奇数阶（固定中心 + 中心条），在 4 阶骨架上加层。
- 6/7 阶：中心/棱规模增大 + parity 更多，同法。
- 8/9/10 阶：方法论一致，表和步数更大，重点是性能（状态空间爆炸，需更聪明的降阶/剪枝）。

**交付**：每阶一批，侧载可测对应阶数引导。

---

## ▶ 第 4 批 —— 输入体验 + 手势 + 引导打磨

- **多阶扫码放大识别**：分面/分行放大拍，降低 7-10 阶高密度格子误识。
- **自动合法性校验接入扫码**：认错→提示重拍该面。
- **手势模式**（v2 坑规避）。
- **新手图卡 + 公式库接入**（`GuideTier` 已有，`FormulaLibrary` 闲置待接）。

**交付**：v 若干批，侧载可测。

---

## ▶ 第 5 批 —— 数据 & 商业化（需 688 后激活，代码先留）

- **iCloud 云同步**：`CloudStore` no-op → 真同步，多设备/换机。
- **真实内购**：StoreKit 2 接 `IAP_实施说明.md` 骨架 + App Store Connect 配商品 + 恢复购买（审核强制）+ Small Business Program(15%)。

**交付**：付费后激活，unsigned 无法真测内购。

---

## ▶ 第 6 批 —— 上架合规 & 最终质量（688 后）

- 隐私政策最终版 + App Privacy Label + 截图/关键词/描述。
- 性能压测（多阶渲染瓶颈、后台求解）、崩溃采集。

---

# 四、杰哥最关心的问题（Q&A 结论区）

**Q1：4-10 阶能像 3 阶一样"下一步引导至还原"吗？**
能。用"分阶段 + 每步实时算下一步"封装成跟 3 阶一样的引导壳，体验零差异。

**Q2：会"走到最后告诉我还原不了"吗？**
不会（结构性排除）。三道闸：① 入口合法性校验，非法直接拦下重拍；② 阶段自检+回溯，有错最多回上一存档点；③ 每步基于真实最新状态算，合法状态永远推向可解。

**Q3：为什么不能一个晚上全做完？**
4-10 阶降阶引擎 = 算法级工程（≈现 App 体量 2-3 倍）。散在一晚会做成一堆 bug 毁掉 v14。分批每批可验、不返工，才稳妥。

---

# 五、工作量预估（诚实版，非承诺工期）

| 批次 | 内容 | 相对规模 |
|---|---|---|
| 第1批 | 2阶UI+引导壳+数据地基 | 现 App +30%（一晚上可完成） |
| 第2批 | 4阶引擎+三道闸 | ≈ 现 App 一半到一倍的独立工程量 |
| 第3批 | 5-10阶延伸 | 每阶再 +30-60%，逐阶递减（复用框架） |
| 第4批 | 扫码/手势/引导 | +30% |
| 第5批 | iCloud+内购 | +20%（需688后激活） |
| 第6批 | 上架合规 | 文档/小改 |

**结论**：2-10 阶 + 上架前全功能 = **数周的专业研发量**。分批推进是唯一不返工、每步可验的路。今晚交付第 1 批。
