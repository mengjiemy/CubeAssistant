# 魔方学院 · 688 元账号上架前「完整功能」方案

> 版本：v1 ｜ 日期：2026-09-08 ｜ 定位：从当前 v14 到「付费 ¥688 开发者账号 → App Store 上架」之间，所有该做、能做的功能全量清单。
>
> 两个版本并排：**大白话**（杰哥看，直说人话）在上，**技术版**（研发执行清单）在下。每项都标了「做不做 / 先做后做 / 谁挡路」。

---

## ⚠️ 先把最关键的认知钉死

**¥688 是苹果开发者账号的年费**（上架 App Store 的硬性门槛，一年一缴），**不是** App 里卖东西的价格。它本身不产生任何功能——它是「你能把 App 摆上 App Store 让 iPhone 用户下载」的门票。

所以「688 元之前能实现的」，正确的理解是：**在花这 688 之前，本地/侧载阶段就该把功能和 Bug 打磨到能上架的程度**；交钱 688 之后主要做的是「接苹果正式签名 + 配置内购 + 过审」这类纯手续，功能基本不能再大改了（一改就要重新提审）。

**结论**：功能研发全部要在「交 688 之前」做完。下面这份就是完整清单。

---

# 一、总览（一眼看懂）

| 阶段 | 内容 | 挡路的东西 | 能否侧载测 |
|---|---|---|---|
| **A. 补齐玩法引擎** | 4-7 阶降阶法、2 阶接 UI | 工作量大（最难） | 能 |
| **B. 体验打磨** | 手势、扫描纠偏、新手引导 | 手势坑/真机摄像头 | 手势能/摄像头部分能 |
| **C. 数据与账号** | iCloud 云同步、多设备 | 需真开发者账号 | 侧载不能（免费账号无 iCloud 能力） |
| **D. 真实内购** | StoreKit 收钱、恢复购买 | 需真账号+沙盒 | 侧载不能 |
| **E. 上架合规** | 隐私、元数据、审核 | 需真账号 | 文档侧载可备 |
| **F. 最终质量** | 性能、崩溃、压测 | 需真机 | 能 |

---

# 二、A 组 · 补齐玩法引擎（先啃，是价值核心）

## A1. 4-7 阶引擎（降阶法）

### 🗣 大白话
你问的「4-7 阶能不能像 3 阶那样解」——**不能，但有一条已铺好的路**。
3 阶那个求解器（Kociemba）只认「中心固定、棱块独立」的结构。4-7 阶没固定中心、棱是拼出来的，直接喂给它它会懵。

但全世界的解法是同一个套路：**先把高阶「降」成 3 阶**——把每面同色的中心块拼成一块（4 阶每面 4 个中心拼 1 个「大中心」）、把同色的一组棱拼齐（4 阶 2 个拼 1 条、5 阶 3 个拼 1 条）。拼完这颗魔方在骨子里就等于一个 3 阶，**最后一步用你现成的 3 阶求解器收尾**。
所以不是白写，**你已验证的 3 阶引擎一个都不用丢**。

### 🔧 技术版
- 阶段 1（4 阶）：实现 `Order4State`（96 facelet / 24 中心 / 24 棱），坐标用半整数（`CubeGeometry.coordHalf` 当前只返回整数范围，偶数阶需扩展——代码注释已明示）。
- 阶段 2（5 阶）：奇数阶多一层「固定中心 + 中心条」，在 4 阶骨架上叠加。
- 阶段 3（6-7 阶）：复用降阶框架 + 更大的置换表。
- **降阶三子步**：① 中心合并（`mergeCenters`）② 棱合并（`mergeEdges`）③ 归并到 3 阶坐标 → 复用 `KociembaSolver.solve` 收尾。
- 单步最优非必需：高阶实时性优先，先给「能解」，再优化步数。
- 工作量：≈ 前 14 版之和，需分批，4 阶先啃。

### 📋 先做 4 阶还是先做 2 阶 UI？见 B 组 A2

---

## A2. 2 阶接入 UI（逻辑已就绪，就差界面）

### 🗣 大白话
2 阶的解算器（`Cube2x2`）这版已经写好了、测试全过、又快又准。但 App 界面里还点不出来——它只是个「幕后引擎」。要做的是把「选 2 阶/3 阶」的入口放上首页，点 2 阶时界面/魔方/计时/还原指引都切到 2 阶去。这是**增量小、最稳、最快见效果**的一件。

### 🔧 技术版
- `CubeModel` 已留 `order` 字段（默认 3），接上「阶数选择器」驱动重建。
- 3D 渲染 `Cube3DView`：按 order 生成 cubelet 集合（2 阶 = 8 块，无中心）。
- 转动表：2 阶用 `Cube2x2` 的 24 facelet 置换（已验证）。
- 还原指引 `SolveSession`：2 阶走 `Solver2x2.solve`。
- 计时 / 历史 / 趋势 / 备份：`SolveRecord` 需加 `order` 字段，跨阶统计分开。

---

# 三、B 组 · 体验打磨（价值高、风险可控）

## B1. 手势模式

### 🗣 大白话
现在转魔方全靠点按钮（6 个面 × 顺逆）。高手想要的是「直接上手拨」：短按选中一层 → 手指一划就转。这是把「练习工具」做成「真魔方手感」的关键，但有个老坑——自定义手势容易被页面的滚动抢占，之前 v2 就栽过。

### 🔧 技术版
- 与 `allowsCameraControl` 并存需切手势识别器优先级（`gestureRecognizerShouldBegin` + `require(toFail:)`）。
- 模式开关：按钮模式 / 手势模式，互斥，避免手势与相机控制抢。
- 转层语义沿用 v8 定稿「选层 → 顺逆」，不打回「横竖」旧坑。

---

## B2. 扫描识别朝向自动纠偏

### 🗣 大白话
现在扫码要你正对着魔方一面拍，偏了会认错颜色，认错了得手动点色块一个个改。想做的：App 自己判断「这面是不是歪了/朝向对不对」，自动校正，减少手动改。

### 🔧 技术版
- 加「整体朝向检测」（根据色块布局推断当前是 U/R/F 哪一面视角）。
- 识别后自动旋转校正而非让用户重拍。
- 依赖真机摄像头，侧载部分能测（需要实拍）。

---

## B3. 新手分阶引导图卡（把已有的档位做扎实）

### 🗣 大白话
档位（新手/中文/专业）已经接进去了，但「新手」档只改了文字，还没有真正的配图步骤卡。给完全不会的人，光看文字不够，要做成「图 + 大白话 + 当前该转哪」的傻瓜式引导。

### 🔧 技术版
- `GuideTier` 已有，`FormulaLibrary`（公式库）目前闲置——把库接进学习 Tab，按档位出「分步公式卡」。
- 配合 A 组求解 3D 预览，做「本步高亮 + 下步预览」的真引导。

---

# 四、C 组 · 数据与账号

## C1. iCloud 成绩云同步（真账号后才通）

### 🗣 大白话
换手机成绩不丢、多设备同步。本地备份（导出 JSON）已经做了，但那是「手动」，这要做成「自动云」。注意：**免费侧载账号开不了 iCloud 能力，这功能得等你真付了 688 才通**，但代码现在可以先接好，静默失败不碍事。

### 🔧 技术版
- `CloudStore.swift` 目前是 no-op 预留，接 CloudKit 真同步（`NSUbiquitousKeyValueStore` 或 CKRecord）。
- 补「登录免费账号时静默降级、付费后自动启用」。
- 本地已有一份数据（`SolveRecord`），需设计合并策略（本地优先 / 云优先 / 按时间戳）。

---

# 五、D 组 · 真实内购（本组要 688 后才能真跑）

## D1. StoreKit 真实收钱

### 🗣 大白话
现在定价是「2 阶免费、3-7 阶内购解锁」。真做内购得先有苹果开发者账号、在 App Store Connect 后台把「3 阶 ¥10、全阶 ¥50」这些商品建好、再用真机 + 沙盒账号测支付流程。**侧载的 unsigned 包过不了内购验证，测不了**。代码骨架已留，付费后补真实接入 + 恢复购买入口（审核强制）。

### 🔧 技术版
- 用现有 `IAP_实施说明.md` 的 `StoreManager.swift` 骨架接 StoreKit 2。
- App Store Connect 建 Product：`unlock3..unlock7` + `unlock_all`。
- 加「恢复购买」（`Transaction.currentEntitlements`），过审强制。
- 申请 Small Business Program（年流水 <$1M → 抽成 30%→15%）。

---

# 六、E 组 · 上架合规（688 后一次性准备）

## E1. 隐私政策 & 审核材料

### 🗣 大白话
上架审核要你回答「你收哪些数据、怎么用」。我们 App 主要**本地存储 + 可选 iCloud**，隐私政策已写过一版（准确反映了本地存储），审核前再按苹果问卷逐条核对 + 补 App Store 截图文案。

### 🔧 技术版
- `PRIVACY_POLICY.md` 已有，上架前更新为最终版并挂在 App 内「设置→隐私政策」。
- 补 App Privacy「Nutrition Label」申报（本地数据不采集则勾「No data collected」）。
- 备 6.7/6.5 寸截图 + 关键词 + 描述。

---

# 七、F 组 · 最终质量（上架前必过）

## F1. 性能 / 崩溃 / 压测

### 🗣 大白话
免费用户最容易被「卡、闪退」劝退。要做：转魔方流畅不卡、连续打乱/求解不闪退、切阶数不崩。真机上跑一轮「连续还原 50 次」之类的压力测试。

### 🔧 技术版
- 用 Instruments 查 SceneKit 渲染瓶颈（多阶 cubelet 增多时）。
- 高阶求解放后台线程（3 阶已做，4-7 阶同样处理）。
- 补 UI 测试 / 崩溃日志采集。

---

# 八、最终研发优先级（建议顺序）

> 原则：**先做能侧载测、且是卖点核心的**；**把要 688 才能测的尽量后置但代码前置留好**。

| 顺序 | 事项 | 侧载能测 | 工作量 | 建议 |
|---|---|---|---|---|
| 1 | **A2 二阶接 UI** | ✅ | 小 | **先做**，最快见效 |
| 2 | **B1 手势模式** | ✅ | 中 | 第二做，手感核心 |
| 3 | **A1 四阶引擎** | ✅ | 大 | 单独攻坚，最难 |
| 4 | **A1 五/六/七阶** | ✅ | 大 | 4 阶通了就顺延 |
| 5 | **B3 新手图卡 + 公式库接入** | ✅ | 中 | 补体验 |
| 6 | **F1 性能压测** | ✅ | 中 | 上架前收尾 |
| 7 | **C1 iCloud 云同步** | ⚠️ 免费号不通 | 中 | 代码先接好 |
| 8 | **D1 真实内购** | ❌ | 中 | **688 后**做 |
| 9 | **E1 上架合规** | ❌ | 小 | **688 后**做 |
| 10 | **B2 扫描纠偏** | ⚠️ 部分 | 中 | 有真机随时可插 |

**一句话路线**：先把 2 阶界面和手势做了（快、稳、能测）→ 狠啃 4 阶降阶引擎（最难、是内购价值核心）→ 5-7 阶顺延 → 打磨引导和性能 → 花 688 后接 iCloud + 真内购 + 上架。

---

## 附：杰哥最关心的那个问题（结论版）

**Q：4-7 阶能像 3 阶一样求解吗？**
**A：不能直接，但能降阶后复用。** 4-7 阶先做「中心合并 + 棱合并」把自己降成等效 3 阶，最后一步跑你已验证的 `KociembaSolver`。不是重写求解器，是加一层预处理。4 阶是最难的（偶数阶无固定中心），啃下它，5/6/7 阶是同一方法论的延伸。
