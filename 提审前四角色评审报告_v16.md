# 魔方学院 · 提审前四角色评审报告（v16-夜批3）

> 评审人：产品 / 设计 / 研发 / 测试 四角色（mini-app-dev-loop 流程）
> 评审对象：当前 `main`（v16 第 3 批 28c7827 已提交）
> 目标：对照「原型 v4」+ Apple HIG + 审核硬门槛，列出**进 App Store 审核前必须清零的问题**，按「会拒审 / 严重体验 / 建议」分档。
> 结语：「求解」（高阶一键解）本轮明确不做，见末节 C。

---

## 一、审核硬门槛（不解决 = 直接拒审 / 无法上架）

| # | 问题 | 现状（代码证据） | 角色判定 | 整改 | 挡路 |
|---|---|---|---|---|---|
| G1 | **无 App 图标（Asset Catalog）** | 全工程无 `.xcassets`，无 `ASSETCATALOG_COMPILER_APPICON_NAME` | 审核 → **必须拒**：无图标无法提交 | 建 `AppIcon.appiconset`（1024 无透明），真账号提审前必须做 | 需设计出图 |
| G2 | **Camera 用途声明文案太技术化** | `NSCameraUsageDescription` 已声明但文案偏「自动识别贴纸颜色」 | 审核 / 用户体验 | 改成大白话并加「仅本机处理不联网」已含；建议补「不会保存相册」更稳妥 | 文案 |
| G3 | **版本号与实际 UI 不符** | Info.plist / MARKETING_VERSION = 1.0；原型「我的→版本」文案为 v2.0 | 一致性 | 统一（首版上架用 1.0，UI 里读 Bundle 版本而非写死） | 无 |
| G4 | **横竖屏声明与 UI 不符** | Info.plist 允许 LandscapeLeft/Right，但 4-Tab 界面全为竖屏设计 | 审核挑刺 + 用户横屏错位 | 若只支持竖屏，Info.plist 只留 Portrait | 无 |

> 结论：G1 是**唯一不可绕过**的硬阻塞，且必须在「付 688 / 真账号」之前用真环境验证；其余 G2-G4 是零成本小修。

---

## 二、严重体验 / 功能闭环（不修会明显掉分或被吐槽）

### P1（产品 / 研发）——本批刚补：学习课程「跟随阶数」
- ✅ 已实现：`CourseStage` 加 `order`，`courses(for:)` 按阶返回课程（2 角块 / 3 层先 / 4 偶数降阶含 parity / 5 奇数降阶 / 6-10 高阶总览奇偶参数化）；学习中心标题与课程随当前魔方阶数实时切换；进度 key 带阶数（每阶独立）+ 旧 3 阶 key 迁移读回。
- ⚠️ 待真机验证：切换阶数后学习中心是否即时刷新（`session` 是 `@ObservedObject`，`order` 为 `@Published`，理论上会随 Home 切阶后重算）；详情 sheet 内课程标题「第 N 课」跨阶重复是否误导（每阶都从第 1 课起）。

### P2（产品）——「我的」页计数与「学习」页进度口径
- 现状：`MineView` 的「已学 N/N 课」已改为跟随当前阶数（本轮）。**注意**：若用户分别学过 3 阶与 4 阶，切换阶数看到的进度会「变来变去」，可能被误读为数据丢失。建议页面上加一行小字「学习进度按当前阶数统计」。

### P3（设计 / 体验）——间距未落 8pt 网格、触控目标偏小
- 代码证据：全文件大量 `padding` 用 12/10/14/6/4/2/18（非 8pt 网格）；触控元素硬编码 20–40pt（HIG 要求 ≥44pt，见参考）。这些是「看起来有点糙」的主因。
- 整改方向：本轮不追求全量重排（改大易回归），但**先把可点元素（返回/收藏/菜单行/chip）垫到 ≥44pt 热区**，间距抽成常量。**详见《设计优化》补充说明**。

### P4（研发）——硬编码强调色泛滥
- 现状：`Color(red:0.04, green:0.52, blue:1.0)`（系统蓝）散落 5+ 处手写，未抽常量 → 改主题要全局搜。
- 整改：抽 `AppColor.accent`（= `.blue` 或语义 `Color.accentColor`），一处定义全局引用。

### P5（研发）——`CourseStage.courses(for:)` 目前每调一次重算
- 现状：`progress` 是计算属性、每次 body 重算读 UserDefaults。列表量小（每阶 ≤5）可接受，但不建议在 SwiftUI body 里做重活。
- 整改（低成本）：把 `courseSection` 的 `courses` 提到 `currentCourses` 缓存（已做），进度读取保持轻量即可。

---

## 三、建议优化（有时间再打磨，非阻塞）

### D1（设计）——空态 / 加载态覆盖不全
- 主页未打乱时、扫描无摄像头权限时、历史为空时：部分页面只有占位文字，未做统一的「图标 + 主文案 + 行动按钮」空态卡（原型对空态较讲究）。

### D2（产品/研发）——收藏公式是「假入口」
- 「我的收藏公式 / 还原指引页收藏」在代码里是 `backupMessage="收藏功能即将上线"` 的占位。提审前要么删掉入口、要么做真收藏（把指引页公式写进 UserDefaults 收藏数组）。**半成品入口比没有更伤体验**。

### D3（产品）——成就「速度之星」等门槛
- 原型成就含「收藏 10 个公式」，若收藏功能暂不做，此成就会永久锁死 → 建议本轮把「收藏达人」成就暂时隐藏或改为可达成项。

### D4（研发）——数据迁移版本化
- `BackupStore` 已有 schemaVersion 迁移器（v14 成果），保持。后续若加收藏/配色，继续走版本化，勿裸加字段。

### D5（设计）——扫猫页与「确认/编辑」闭环
- 原型要求扫描后进入「确认页：6 面 Tab + 点方块换色 + 开始还原」。本轮扫描页已重写，需真机实测摄像头 + 手动兜底改色路径是否都能走到「开始还原」。

---

## 四、测试视角验收清单（真机必测，逐条勾）

> 本地 swiftc parse 通过 + 模型层 143 用例全绿；UI 层与手势只能靠真机/CI。以下为**必须真机划一遍**的项目：

1. **切阶联动画**：主页切 2→10 阶各一次 → 学习中心课程标题与列表是否跟随刷新（本轮新增）。
2. **2 阶极简指引**：2 阶打乱 → 求解 → 「我转好了下一步」能否走完。
3. **3 阶回归**：打乱/手动转/求解指引/还原判定与 v15 一致（本轮动了 `CourseStage`，3 阶课程内容应一字未改——已保留原 5 课）。
4. **4-10 阶可玩**：打乱 → 手动转（含选面转/手势滑）→ 还原判定；整层转动即时重绘是否可接受（无动画，瞬时变色——已标注为取舍）。
5. **学习课程点开 + 标记完成**：任意阶课程 → 详情 sheet → 标记完成 → 返回列表进度「100%/已完成 ✓」；换阶再回，进度不串（key 已隔离）。
6. **扫描**：拍照/相册双卡 → 逐面引导 → 确认页换色 → 开始还原。
7. **视角归位按钮**（v16-第1批修复）：拖动 3D 后点右上角 scope，是否回正。
8. **「我的」页**：统计卡、资料编辑、导出/导入备份。
9. **触控热区抽查**：返回箭头、菜单行、chips 是否 ≥44pt 好点。
10. **冷启动 + 快速切 4 Tab**：无白屏/闪退/状态错位。

---

## 五、本轮未做（老板已拍板）

- **C · 高阶自动求解**（4-10 阶降阶自动解）＝数周算法工程，本轮明确**不做**，仅保留「高阶可手动玩 + 打乱/还原判定 + 教程」。高阶求解功能后续单独立项，与 UI 无耦合、不影响上架本版（上架本版若高阶只能手玩，需在 App 内文案把「高阶」定位为「练习/教程」，避免用户以为点了「求解」能一键解——`solve()` 对高阶已提示「暂不支持一键求解」，已兜底）。

---

## 六、下一步建议顺序（老板审核用）

| 顺 | 事项 | 成本 | 前置 |
|---|---|---|---|
| 1 | 配 App 图标（设计出图 + 建 Asset Catalog） | 中 | 无（G1，提审必经） |
| 2 | 本轮代码已在 CI（28c7827 绿后）出 v16 ipa 真机回归 | — | CI |
| 3 | 真机勾选「四、测试清单」10 项 | 中 | 出包 |
| 4 | 砍/做实「收藏公式」假入口（D2/D3 二选一） | 小-中 | 无 |
| 5 | 触控热区垫到 ≥44pt + 强调色抽常量（P3/P4） | 小 | 无 |
| 6 | 空态/横屏/版本一致性收尾（D1/G3/G4） | 小 | 无 |
| 7 | 付 688 → 配隐私/元数据/截图/审核材料 | — | 真账号 |

**一句话给老板**：硬阻塞只有 1 个（App 图标没建）；本轮学习页各阶教程已落地并随阶切换；剩下的主要是「收藏假入口 + 触控热区 + 真机勾 10 项」，没有会翻车的雷。
