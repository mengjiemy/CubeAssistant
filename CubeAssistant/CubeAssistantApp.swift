import SwiftUI

/// App 入口。把它作为 Xcode 工程的主文件即可（确保 Target 里只有这一个 `@main`）。
@main
struct CubeAssistantApp: App {
    var body: some Scene {
        WindowGroup {
            ContentView()
        }
    }
}
