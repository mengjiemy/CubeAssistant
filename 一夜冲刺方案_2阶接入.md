# 魔方学院 · 一夜冲刺交付方案（现在 → 明早 10:00）

> 杰哥要的是：熬一整夜，明早 10 点给我 ipa。你能做到什么程度？这部分的方案给我。
>
> 这份就是「一夜 15 小时（约 19:20 → 次日 10:00）」的**真实可交付边界 + 分批执行计划 + 明早验收对照清单**。不画饼，每一条都能明早验证。

---

## 0. 一句话承诺

**一夜能做到**：3 阶完全不受损 + **2 阶真正可玩**（阶数切换 / 2 阶 3D 渲染 / 打乱 / 手动转 / 还原引导）+ 成绩跨阶分离 + 完整分批方案文档。
**一夜做不到**：4-10 阶降阶引擎（数周算法工程，硬塞必翻车，不骗你说能做）。

> 已核实的技术依据（全部读代码确认，非拍脑袋）：
> - `CubeState`/`movePerms`/`Cube3DView.faceMap`/`CubeGeometry` 深度硬编码 3 阶（54 格 / 26 块 / 每面 9 格）。
> - `Cube2x2` 是**独立的另一套**：24 面片，通过「映射到 3 阶角贴纸 → 复用 movePerms」转动，求解用 `Solver2x2`（角块双向 BFS）。
> - 因此 2 阶接入 UI = 给 `Cube3DView` 加**隔离的 2 阶渲染路径** + `CubeSession`/`ContentView` 加阶数分支。**3 阶路径完全不碰 = 零风险**。

---

## 一、一夜冲刺的分批执行计划

### 🟢 第 0 批：交付承诺书 + 冻结基线（约 30 分钟）
- 本方案文档定稿。
- 跑一遍本地完整测试（当前 68 项），冻结 v14 基线为「零回归基准」。
- 打 tag `v14-baseline`，确保任何改动可回退。

### 🟢 第 1 批：多阶数据地基（2-3 小时，纯增量，3 阶零影响）
- `SolveRecord` 加 `order: Int = 3`（默认 3，老记录自动兼容）。
- 历史持久化加 `schemaVersion` 迁移器（老数据无 order → 补 3）。
- 成就 `totalSolves/bestTime/averageTime` 支持按 `order` 过滤（先保留全局，为跨阶统计铺路）。
- 本地测试：老数据反序列化兼容性 + 新记录带 order。

### 🟢 第 2 批：2 阶 3D 渲染（隔离路径，3-4 小时）
- 给 `Cube3DView` 加 `order: Int` 参数。
- 加 **`buildCube2x2(facelets:)`**：2 阶 = 8 个角块（坐标 ±0.5，无中心/棱），每块 3 个可见外表面贴 sticker。
- 加 **2 阶 faceMap**：24 面片 → 8 块坐标 + 面方向（映射关系复用 `Cube2x2` 的角贴纸语义）。
- 相机默认距离拉近（2 阶更小，否则显得远）。
- **3 阶路径 `buildCube(facelets:)` 一字不改**——只新增，不修改。

### 🟢 第 3 批：CubeSession + UI 阶数切换（3-4 小时）
- `CubeSession` 加 `order` + `setOrder(_:)`，切 2/3 阶时重置状态、清指引。
- 首页加「2 阶 / 3 阶」切换入口（放主页顶部）。
- 转层按钮：3 阶 6 面照旧；2 阶转动仍走同一套 `Move`（2 阶无中层，6 面 × 顺逆即可，转动用 `Cube2x2` 应用）。
- 打乱：2 阶用 `Cube2x2.scrambled`，3 阶照旧。
- 还原指引：2 阶走 `Solver2x2.solve`，走完映射成文字轨道（复用现 `SolveSession` 结构但换求解器）。

### 🟢 第 4 批：整合 + 本地测试 + 修回归（2-3 小时）
- 跑全部本地逻辑测试（含新增 2 阶 UI 无关的引擎测试）。
- 语法 parse 全工程。
- 重点回归：切回 3 阶后所有旧功能表现与 v14 完全一致。

### 🟢 第 5 批：提交 + 触发 CI + 拿 ipa（1-2 小时，看 CI 排队）
- commit + push → CI（约 3-5 分钟）→ 下载 artifact → 命名 `魔方学院_demo_v15_unsigned.ipa`。
- 若 CI 失败：回退到 v14-baseline 修复，重试。

### ⏱ 缓冲：2-3 小时（明早 7-10 点）
留给编译错误、CI 波动、回归修复。

---

## 二、明早 10 点验收对照清单（杰哥逐条打勾）

装 `魔方学院_demo_v15_unsigned.ipa` 后：

| # | 验收点 | 操作 | 预期 |
|---|---|---|---|
| 1 | 2 阶可选 | 首页顶切换 2 阶 | 3D 变成 8 块小魔方（无中心/棱） |
| 2 | 2 阶打乱 | 2 阶下点打乱 | 8 块颜色打乱，可再打 |
| 3 | 2 阶手动转 | 选面转 | 对应角块转，6 面 × 顺逆可转 |
| 4 | 2 阶还原引导 | 打乱后点求解 | 出一串步骤，一步步跟到「已还原」 |
| 5 | 2 阶还原成功 | 按指引转完 | 自动判定还原、计时停表 |
| 6 | **切回 3 阶无损** | 切回 3 阶 | 所有旧功能与 v14 完全一致（重点！） |
| 7 | 成绩带阶数 | 2 阶还原一次 | 战绩记录带 order（历史/备份里有阶数） |
| 8 | 老数据兼容 | 升级后看旧成绩 | 历史不丢、正常显示 |

**明早若 1-8 全过 → 一夜目标达成。** 若 6（3 阶无损）出问题 → 立刻回退 v14 保底，绝不让 2 阶拖垮已验证的 3 阶。

---

## 三、为什么"4-10 阶降阶引擎"一夜做不完（透明交代）

- 3 阶 Kociemba 靠「固定中心 + 独立棱」结构。4-10 阶无固定中心、棱是拼的，Kociemba 直接失效。
- 高阶解法 = 「中心合并 + 棱合并」把高阶降成等效 3 阶，再复用 Kociemba。这是**数周级算法工程**（每阶一套合并表 + parity 处理 + 多阶合法性校验）。
- 这一夜把它硬塞进去，只会产出 bug、拖垮已验证的 3 阶 + 2 阶。**诚实不骗你：4-10 阶是明早 10 点之后，按方案文档分批推进**，2 阶通了、框架定型了，4 阶最难、啃下它 5-10 阶是同一方法论延伸。

---

## 四、交付物清单（明早 10 点）

1. `魔方学院_demo_v15_unsigned.ipa`（可装，含 2 阶 + 3 阶）
2. 本承诺书（对照验收）
3. 若时间够：附 `研发方案_2到10阶完整App.md`（全量分批，明早后推进依据）

---

**结论**：这一夜，我做到「3 阶无损 + 2 阶真正可玩 + 多阶数据地基」。4-10 阶降阶引擎明早 10 点后按方案分批做。这是不毁 v14、真往多阶走的唯一稳妥路径。
